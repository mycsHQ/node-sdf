language: node_js
cache:
  directories:
    - ~/.npm
node_js:
  - "8"
  - "9"
before_install:
  - sudo apt-get -qq update
script:
  - npm test
after_success:
  - npm run travis-deploy-once "npm run semantic-release"
branches:
  except:
    - /^v\d+\.\d+\.\d+$/